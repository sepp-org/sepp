---
title: "Configuration reference"
description: "Every sepp.toml key with type, default, hot-reloadable flag, and description."
---

{/* SCAFFOLD — layout only, no prose written yet. */}
{/* TODO(content): Exhaustive key tables (TypeTable-friendly) with type / default / hot-reloadable. Must include max_message_bytes, default_max_attempts, max_attempts_ceiling, default_priority, max_queue_depth tri-state, prometheus_listen_addr, export_interval_ms. Cross-validation rules (max_payload_bytes <= max_message_bytes; default_max_attempts <= max_attempts_ceiling; max_queue_name_bytes <= 65535). [[queues]]-only key: allowed_job_types. Env mapping: SEPP_<SECTION>__<FIELD>, env wins over file. */}

## [server]

## [auth]

## [limits]

## [storage]

## [logging] / [tracing] / [metrics]

## [[queues]] overrides

## Environment variable mapping

