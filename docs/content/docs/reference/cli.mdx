---
title: "CLI"
description: "The sepp command, its config subcommand, and the flags and environment variables it reads."
---

{/* SCAFFOLD — layout only, no prose written yet. */}
{/* TODO(content): CLI reference. Default run mode; `sepp config example`; --config / SEPP_CONFIG resolution (defaults to ./sepp.toml); SIGTERM/Ctrl-C; exit codes. */}

## sepp (run the server)

## sepp config example

## --config and SEPP_CONFIG

## Signals and exit codes

