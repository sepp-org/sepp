---
title: "Server capabilities"
description: "The GetServerInfo response: version, protocol versions, limits, and feature flags clients use to self-validate."
---

{/* SCAFFOLD — layout only, no prose written yet. */}
{/* TODO(content): GetServerInfoResponse reference. Include server_time_ms (clock-skew), supported_protocol_versions, restricts_encodings/allowed_encodings, and the advertised maxima (payload, custom-map, name bytes, schedule horizon, enqueue batch, reserve batch/queues, wait timeout, lease duration). NOTE max_message_bytes is NOT advertised. Map each advertised limit -> its JobRejection -> its config key. */}

## Calling GetServerInfo

## Version and protocol fields

## Advertised limits

## Feature flags: strict_queues, dead-letter retention

## Using capabilities for local validation

