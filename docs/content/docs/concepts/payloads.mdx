---
title: "Payloads & custom metadata"
description: "Opaque payload bytes with an encoding hint, the custom primitive map, and the small-control-data policy."
---

{/* SCAFFOLD — layout only, no prose written yet. */}
{/* TODO(content): bytes + encoding hint, PrimitiveValue custom map, small-control-data-only policy (large blobs -> client object storage + URL in custom map). Encoding allow-lists (restricts_encodings/allowed_encodings). Honors the payload-schema + payload-size memories. */}

## Opaque payloads and encoding hints

## The custom value map

## Payload size policy

## Large blobs and object storage

## Encoding allow-lists

