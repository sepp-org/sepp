---
title: "Queues & strict mode"
description: "Named queues, auto-create versus strict declaration, and per-queue configuration overrides."
---

{/* SCAFFOLD — layout only, no prose written yet. */}
{/* TODO(content): strict_queues, on-first-use auto-create, [[queues]] override semantics (merge over defaults), depth caps. Note allowed_job_types is a per-queue-only override (no global key, not advertised in GetServerInfo). Config keys live in Reference. */}

## Named queues

## Auto-create vs strict mode

## Per-queue overrides

## Queue depth and caps

## Choosing a queue layout

