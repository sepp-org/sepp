---
title: "Idempotency & deduplication"
description: "How idempotency keys deduplicate enqueues within the dedup window."
---

{/* SCAFFOLD — layout only, no prose written yet. */}
{/* TODO(content): CANONICAL idempotency hub. dedup_window_ms, the deduplicated flag, contrast enqueue-side dedup (here) with worker-side idempotent handlers (workers/idempotent-workers). Every other idempotency page should open with a one-line disambiguation linking here. */}

## Idempotency keys

## The dedup window

## Deduplicated enqueue responses

## Enqueue-side dedup vs idempotent handlers

## Limits and tuning

