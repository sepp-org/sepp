---
title: "Connecting & authenticating"
description: "Open a connection, present an API key, and enable TLS from the client side."
---

{/* SCAFFOLD — layout only, no prose written yet. */}
{/* TODO(content): Shared connection how-to (LangTabs). API-key metadata, client TLS, keepalive, calling GetServerInfo (and server_time_ms for clock-skew detection). */}

## Open a connection

## Provide an API key

## Connect over TLS

## Connection keepalive and timeouts

## Discovering server capabilities

