---
title: "Client SDKs"
description: "First-party SDKs (Rust, Python, Go), what they share, and how to pick one."
---

{/* SCAFFOLD — layout only, no prose written yet. */}
{/* TODO(content): SDK directory + merged connecting + error-handling. First-party SDKs are Rust (F:/sepp-rs), Python (F:/sepp-py) and TypeScript/Node (F:/sepp-js). Points to the per-task LangTabs in Producers/Workers. ABSORBED connecting: API-key metadata, client TLS, keepalive, calling GetServerInfo (server_time for clock-skew). ABSORBED error-handling overview: gRPC status map vs JobRejection and when to retry — defer the full catalogue to reference/rejections. */}

## Official SDKs

## Connecting

## What every client shares

## Errors & retries

## Versioning & compatibility

