---
title: "Install"
description: "Get the sepp binary: prebuilt release, cargo install, Docker image, or build from source."
---

{/* SCAFFOLD — layout only, no prose written yet. */}
{/* TODO(content): Install paths as sibling options (release, cargo, ghcr.io/sepp-org/sepp Docker, source). protoc is a hard build prerequisite; verify with sepp on PATH. */}

## Download a release binary

## Run with Docker

## Build from source

## Install protoc

## Verify the install

