---
title: "Authentication & TLS"
description: "Enable API-key authentication and serve gRPC over TLS."
---

{/* SCAFFOLD — layout only, no prose written yet. */}
{/* TODO(content): Security how-to. [auth] api_keys tri-state (omitted = allow all; non-empty list = only those; empty [] = deny all). TLS needs both tls_cert_path and tls_key_path or neither. Always use keys over TLS. Key rotation IS hot-reloadable (verified: interceptor.set_keys on reload). */}
{/* TODO(content): Admin UI auth. [admin] keys = named keys with roles (viewer < operator < admin, role defaults to admin); omitted = auth off, loopback binds only. Login with name + key sets an HttpOnly SameSite=Strict session cookie (session_ttl_ms, default 12h); Bearer <key> works for curl. Key rotation hot-reloads and signs sessions out. GET /config redacts auth.api_keys to a count and admin.keys to names+roles; admin.keys cannot be edited via the admin API. Non-loopback bind requires keys; the listener is plain HTTP, so terminate TLS in a fronting proxy. Mutations are audit-logged under the sepp::audit tracing target with actor + role; failed logins audit as session.login_failed. */}

## API key authentication

## Key allow-list semantics

## Enabling TLS

## Keys over plaintext warning

## Rotating keys with hot reload

## Admin UI authentication

## Admin roles

## Audit log

