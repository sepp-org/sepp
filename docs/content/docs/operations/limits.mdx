---
title: "Limits & quotas"
description: "Set global and per-queue limits on lease, attempts, payloads, batches, depth, and schedule horizon."
---

{/* SCAFFOLD — layout only, no prose written yet. */}
{/* TODO(content): Limits how-to. [limits] + [[queues]] merge; which limit maps to which rejection; advertised via GetServerInfo. Call out max_message_bytes (whole-request ceiling, >= max_payload_bytes) vs max_payload_bytes; max_queue_depth tri-state (omit = unlimited, 0 = reject all); default_max_attempts/max_attempts_ceiling/default_priority. Exact defaults in Reference. */}

## Global limits

## Per-queue overrides

## Payload vs message size

## Batch and reserve limits

## Depth caps and schedule horizon

