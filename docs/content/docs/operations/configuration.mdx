---
title: "Configuration"
description: "Configure sepp via sepp.toml, SEPP_ environment overrides, and hot reload."
---

{/* SCAFFOLD — layout only, no prose written yet. */}
{/* TODO(content): Config hub. File + SEPP_<SECTION>__<FIELD> env precedence + hot reload + `sepp config example`. Hot-reloadable: [auth] api_keys, [[queues]], strict_queues, and registry-derived limits. Restart-only: listen_addr, db_path, TLS paths, max_message_bytes, all [storage] tuning, and [logging]/[tracing]/[metrics]. Full key tables in Reference. */}

## The sepp.toml file

## Config sections overview

## Environment variable overrides

## Hot reloading

## Generating an example config

