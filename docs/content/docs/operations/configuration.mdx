---
title: "Configuration"
description: "Configure sepp via sepp.toml, SEPP_ environment overrides, and hot reload."
---

{/* SCAFFOLD — layout only, no prose written yet. */}
{/* TODO(content): Config hub + merged limits + durability + storage tuning. File + SEPP_<SECTION>__<FIELD> env precedence + hot reload + `sepp config example`. Hot-reloadable: [auth] api_keys, [[queues]], strict_queues, registry-derived limits. Restart-only: listen_addr, db_path, TLS paths, max_message_bytes, all [storage] tuning, [logging]/[tracing]/[metrics]. ABSORBED limits: [limits] + [[queues]] merge; which limit maps to which rejection; max_message_bytes (whole-request, >= max_payload_bytes) vs max_payload_bytes; max_queue_depth tri-state (omit = unlimited, 0 = reject all); default_max_attempts/max_attempts_ceiling/default_priority. ABSORBED durability: the three persist modes + ~4x buffer trade-off, sweep_interval_ms/sweep_limit, command_queue_capacity (restart-only). ABSORBED storage tuning: fjall cache_size_bytes (~20-25% RAM), max_journaling_size_bytes, max_cached_files, worker_threads, dedup_window_ms (all restart-only). Full key tables in Reference. */}

## The sepp.toml file

## Config sections overview

## Environment variable overrides

## Hot reloading

## Limits & quotas

## Durability & persist modes

## Storage tuning

## Generating an example config

