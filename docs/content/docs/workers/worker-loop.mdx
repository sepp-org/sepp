---
title: "Building a worker loop"
description: "Structure a resilient reserve-process-ack loop with concurrency and graceful shutdown."
---

{/* SCAFFOLD — layout only, no prose written yet. */}
{/* TODO(content): End-to-end worker-loop how-to (LangTabs). Concurrency, lease heartbeats, draining its own in-flight work on shutdown, reconnection. NOTE: the server does not re-queue leased jobs on its own shutdown — they redeliver after lease expiry; link operations/deployment for authoritative server behavior. */}

## Loop skeleton

## Concurrency and prefetch

## Lease heartbeating with Extend

## Graceful shutdown of in-flight leases

## Reconnect and retry

